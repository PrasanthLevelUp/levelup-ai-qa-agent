// Jest Snapshot v1, https://jestjs.io/docs/snapshot-testing

exports[`Sprint 2D.3 — golden: KB → graph → Script Gen → Playwright emits the pinned Playwright spec from the real pipeline: login-valid.spec 1`] = `
"import { test, expect } from '@playwright/test';

test.describe('Valid credentials login', () => {
  test('Valid credentials login', async ({ page }) => {
    // @tc:TC7003
    await page.goto('https://www.saucedemo.com/');
    await page.waitForLoadState('domcontentloaded');

    await page.locator('[data-test="username"]').fill('standard_user');

    await page.locator('[data-test="password"]').fill('secret_sauce');

    await page.locator('[data-test="login-button"]').click();


    await expect(page).toHaveURL(/inventory\\.html/);
    await expect(page.locator('[data-test="title"]')).toHaveText(/Products/i);
  });
});
"
`;
